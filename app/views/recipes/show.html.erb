<h1><%= display_recipe_name(@recipe) %></h1>

<h3>Ingredients:</h3>
  <table>
    <tr>
      <th>Quantity</th>
      <th>Unit</th>
      <th>Name</th>
    </tr>
    <%= render(partial: '/quantities/quantity', collection: @recipe.quantities) || "No Ingredients!" %>
  </table>

<h3>Instructions</h3>
  <ol>
    <%= render(partial: '/instructions/instruction', collection: @recipe.instructions) || "No Instructions!" %>
  </ol>
